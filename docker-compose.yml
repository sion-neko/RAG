services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - chroma_data:/app/chroma
    env_file:
      - .env
    restart: unless-stopped

volumes:
  chroma_data:
